test-cases:
  - test-suite: test_integration_deploy
    test-execution-id: "a"
    test-params:
      charm:
        charm: postgresql-k8s
      neighbor:
        charm: easyrsa
      integration:
        application-a: postgresql-k8s
        application-b: easyrsa
        
  - test-suite: test_integration_deploy
    test-execution-id: "b"
    test-params:
      charm:
        charm: postgresql-k8s
      neighbor:
        charm: easyrsa
      integration:
        application-a: postgresql-k8s
        application-b: easyrsa