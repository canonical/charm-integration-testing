---
# ML Metadata - stores metadata for ML workflows
overrides:
  - provides:
      grpc:
        features: [mlmd-grpc, kfp-api]
      velero-backup-config:
        optional: true
    constraints:
      - '"mlmd-grpc" in features(endpoint[grpc]) or "kfp-api" in features(endpoint[grpc])'
