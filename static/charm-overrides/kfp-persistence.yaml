---
overrides:
  - requires:
      kfp-api:
        features: [kfp-api]
    constraints:
      - '"kfp-api" in features(endpoint[kfp-api])'
      - 'tracks({self}) == tracks(charms(endpoint[kfp-api]))'
      - 'risks({self}) == risks(charms(endpoint[kfp-api]))'
