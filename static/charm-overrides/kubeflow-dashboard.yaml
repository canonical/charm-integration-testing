---
# Kubeflow Central Dashboard - main UI entry point
overrides:
  - provides:
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
    requires:
      kubeflow-profiles:
        features: [kubeflow-profiles]
    constraints:
      - '"kubeflow-profiles" in features(endpoint[kubeflow-profiles])'
