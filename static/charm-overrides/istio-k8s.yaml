---
# istio-k8s is a cluster-wide control-plane singleton for the Istio mesh -- running
# more than one per cluster isn't how it's normally operated, so it's shared across
# every model on a controller rather than deployed per-dependent. This is also the
# ClusterAddonExtension default; the file exists to document the choice explicitly.
addon_scope: cluster
