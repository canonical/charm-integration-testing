---
# KFP Metadata Writer - records pipeline execution metadata to MLMD
listed: true
overrides:
  - requires:
      grpc:
        features: [mlmd-grpc]
    constraints:
      - '"mlmd-grpc" in features(endpoint[grpc])'
