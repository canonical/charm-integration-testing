---
# Remove after: https://github.com/canonical/wordpress-k8s-operator/pull/343
overrides:
  - provides:
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
    requires:
      nginx-route:
        optional: true
      logging:
        optional: true
