---
# ceph-base: Charmhub *bundle* (not a charm) - has no charm metadata/bases, so
# charm-only overrides (e.g. assumes) do not apply and would break live
# override validation against Charmhub.
#
# listed: false -- Excluded from automated bundle building (SQT-1081) to prevent
# pipeline congestion; not currently covered by Charm QA.
listed: false
