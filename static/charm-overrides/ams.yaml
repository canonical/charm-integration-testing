---
overrides:
  - requires:
      lxd-cluster:  # reached out to ams team, this should be a peer relation.
        optional: true
      openfga:
        optional: true
      send-remote-write:
        optional: true
    provides:
      grafana:
        optional: true
