---
# Katib DB Manager - manages database schema and migrations
overrides:
  - provides:
      k8s-service-info:
        features: [katib-service, kfp-api]
    constraints:
      - '"katib-service" in features(endpoint[k8s-service-info]) or "kfp-api" in features(endpoint[k8s-service-info])'
