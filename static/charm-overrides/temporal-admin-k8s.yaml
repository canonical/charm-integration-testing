---
listed: true
overrides:
  - provides:
      admin:
        limit: 1
        optional: false
        features: [admin]
    requires:
      temporal-host-info:
        optional: true
    constraints:
      # admin endpoint must use admin feature
      - bool(endpoint[admin]) => features(endpoint[admin]) == {"admin"}
