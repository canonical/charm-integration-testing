---
overrides:
  - provides:
      admin:
        limit: 1
        optional: false
        features: [admin]
    constraints:
      # admin endpoint must use admin feature
      - bool(endpoint[admin]) => features(endpoint[admin]) == {"admin"}
