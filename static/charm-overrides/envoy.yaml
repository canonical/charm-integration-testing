---
# https://github.com/canonical/charm-integration-testing/issues/282
listed: true
overrides:
  - provides:
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
    requires:
      grpc:
        features: [mlmd-grpc]
    constraints:
      - '"mlmd-grpc" in features(endpoint[grpc])'
