---
overrides:
    # Remove after: https://github.com/canonical/hydra-operator/pull/358
    # Tracks 0.1 and 0.2 use public-ingress (legacy traefik API) but lack internal-ingress.
    # internal-ingress was only added to tracks 0.3 and istio.
  - criteria:
      - any_of:
          - track: '0.2'
          - track: '0.1'
    provides:
      hydra-endpoint-info:
        optional: true
      oauth:
        optional: true
        features: [tls]
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
    requires:
      public-ingress:
        optional: false
        features: [tls]
      admin-ingress:
        optional: true
        features: [tls]
      tracing:
        optional: true
      logging:
        optional: true
    proxies:
      - interface: certificates
        requires: public-ingress
        provides: oauth
    constraints:
      # public-ingress and oauth must agree on tls
      - '("tls" in features(endpoint[public-ingress])) == ("tls" in features(endpoint[oauth]))'
      # if not in dev mode, ingress must use tls
      - not config[dev] => "tls" in features(endpoint[public-ingress])
    # Tracks 0.3 and istio use public-ingress and also expose internal-ingress.
  - criteria:
      - any_of:
          - track: istio
          - track: '0.3'
    provides:
      hydra-endpoint-info:
        optional: true
      oauth:
        optional: true
        features: [tls]
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
    requires:
      public-ingress:
        optional: false
        features: [tls]
      internal-ingress:
        optional: true
        features: [tls]
      admin-ingress:
        optional: true
        features: [tls]
      tracing:
        optional: true
      logging:
        optional: true
    proxies:
      - interface: certificates
        requires: public-ingress
        provides: oauth
    constraints:
      # public-ingress and oauth must agree on tls
      - '("tls" in features(endpoint[public-ingress])) == ("tls" in features(endpoint[oauth]))'
      # if not in dev mode, ingress must use tls
      - not config[dev] => "tls" in features(endpoint[public-ingress])
    # Newer tracks use public-route (new traefik API)
  - provides:
      hydra-endpoint-info:
        optional: true
      oauth:
        optional: true
        features: [tls]
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
    requires:
      public-route:
        optional: false
        features: [tls]
      internal-route:
        optional: true
        features: [tls]
      tracing:
        optional: true
      logging:
        optional: true
    proxies:
      - interface: certificates
        requires: public-route
        provides: oauth
    constraints:
      # public-route and oauth must agree on tls
      - '("tls" in features(endpoint[public-route])) == ("tls" in features(endpoint[oauth]))'
      # if not in dev mode, public-route must use tls
      - not config[dev] => "tls" in features(endpoint[public-route])
