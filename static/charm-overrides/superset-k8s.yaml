---
overrides:
  - requires:
      nginx-route:
        optional: true
    provides:
      grafana-dashboard:
        optional: true
      metrics-endpoint:
        optional: true
    configs:
      superset-secret-key: ["thisISaSECRET_1234"]
    constraints:
      - 'set(config[superset-secret-key])'
