---
configs:
  - criteria:
      track: '4'
    config:
      roles: broker,controller
  - criteria:
      track: '4'
    config:
      roles: broker
  - criteria:
      track: '4'
    config:
      roles: broker,controller
