---
configs:
    # Temporal worker requires the following 3 configurations to be set
    # the charm will go into an active/idle state once these are provided
    # all these values are dummy values for testing purposes
    # nothing is currently deployed at charmqa.canonical.com
  - config:
      host: charmqa.canonical.com
      namespace: charm-test
      queue: test-queue
  - criteria:
      - endpoint_integrated: database
    config:
      host: charmqa.canonical.com
      namespace: charm-test
      queue: test-queue
      db-name: temporal
