configs:
    # Temporal worker requires the following 3 configurations to be set
    # the charm will go into an active/idle state once these are provided
  - config:
      host: charmqa.canonical.com
      namespace: charm-test
      queue: test-queue