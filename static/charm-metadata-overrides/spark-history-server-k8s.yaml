
overrides:
  - requires:
      s3-credentials:
        optional_if:
          - endpoint_integrated: azure-storage-credentials
      azure-storage-credentials:
        optional_if:
          - endpoint_integrated: s3-credentials
      ingress:
        optional_if:
          - none_of:
              - endpoint_integrated: auth-proxy
              - endpoint_integrated: oauth2-proxy
