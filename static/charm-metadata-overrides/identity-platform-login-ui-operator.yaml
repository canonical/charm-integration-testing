---
# Remove after: https://github.com/canonical/identity-platform-login-ui-operator/pull/282
provides:
  ui-endpoint-info:
    optional: true
  metrics-endpoint:
    optional: true
  grafana-dashboard:
    optional: true
requires:
  public-route:
    interface: traefik_route
    optional_if:
      - none_of:
          - endpoint_integrated: ui-endpoint-info
  hydra-endpoint-info:
    interface: hydra_endpoints
    optional: true
  kratos-info:
    interface: kratos_info
    optional: true
