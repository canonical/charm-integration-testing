
# Metacontroller Operator - custom controller framework
overrides:
  - provides:
      # Remove after: https://github.com/canonical/charm-integration-testing/issues/282
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
