
# Envoy proxy - HTTP/gRPC access to ML Metadata
overrides:
  - requires:
      grpc:
        features: [mlmd-grpc]
    provides:
      # Remove after: https://github.com/canonical/charm-integration-testing/issues/282
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
