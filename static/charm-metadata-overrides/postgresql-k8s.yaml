---
# Remove after: https://github.com/canonical/postgresql-k8s-operator/pull/1018
overrides:
  - provides:
      replication-offer:
        optional: true
      metrics-endpoint:
        optional: true
      grafana-dashboard:
        optional: true
      database:
        limit: 1
        optional: true
      db:
        limit: 1
        optional: true
      db-admin:
        limit: 1
        optional: true
    requires:
      certificates:
        optional: true
