---
# Katib DB Manager - manages database schema and migrations
overrides:
  - constraints: |
      ; k8s-service-info endpoint must use katib-service mode
      (assert (=> k8s-service-info (= k8s-service-info_mode "katib-service")))
