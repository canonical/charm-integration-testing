---
# Kubeflow Pipelines UI - web interface for pipeline management
overrides:
  - constraints: |
      ; kfp-ui endpoint must use kfp-ui mode
      (assert (=> kfp-ui (= kfp-ui_mode "kfp-ui")))
      ; kfp-api endpoint must use kfp-api mode
      (assert (=> kfp-api (= kfp-api_mode "kfp-api")))
